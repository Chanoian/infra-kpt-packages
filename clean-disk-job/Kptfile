
apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: lvm-clean-disk
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: this package is used to deploy lvm clean disk job before deploying lvm operator 
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/apply-replacements:v0.1.1
      configPath: apply-replacements-namespace.yaml
